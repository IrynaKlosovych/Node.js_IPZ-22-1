<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Weather Page</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
{{>header}}
<main>
    <section id="weather" class="mt-4">
        <div class="container mx-4 px-4">
            <div class="row">
                <div class="col-4">
                    <div class="list-group">
                        {{#each cities}}
                            <a href="/weather/{{name}}" class="list-group-item list-group-item-action">{{name}}</a>
                        {{/each}}
                    </div>
                </div>
                <div class="col-8">
                    <div class="border rounded">
                        <h5 class="mt-3" style="margin-left: 15px;"> {{#if city}}
                            Погода в місті {{city}} {{else}} <p>Погода в межах Ваших координат</p><p>Широта:{{latitude}}, довгота: {{longitude}}</p>{{/if}}</h5>
                        <div class="d-flex justify-content-center">
                            <img src="https://openweathermap.org/img/wn/{{weather.weather.0.icon}}@2x.png"
                                 alt="Icon">
                        </div>
                        <table class="table table-bordered ">
                            <tr>
                                <td>Тиск</td>
                                <td><p>{{weather.main.pressure}}</p></td>
                            </tr>
                            <tr>
                                <td>Вологість</td>
                                <td>{{weather.main.humidity}}</td>
                            </tr>
                            <tr>
                                <td>Температура</td>
                                <td>{{weather.main.temp}}</td>
                            </tr>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </section>
</main>
{{>footer}}
</body>
</html>